import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  media: {
    height: 140,
    objectFit: 'contain',
    padding: 10
  },
  link: {
    textDecoration: 'none'
  }
});

export default useStyles;
